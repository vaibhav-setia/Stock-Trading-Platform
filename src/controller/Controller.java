package controller;

/**
 * Controller Interface for methods being used by controller
 * to define the flow/start of program.
 */
public interface Controller {
  /**
   * Method to initialise the stock system
   * and start the flow of program.
   */
  void start();
}
